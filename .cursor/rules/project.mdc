---
description: AI衣橱助手 — 技术栈、架构、代码规范与领域模型
globs: **/*
alwaysApply: true
priority: 110
---

# 🔒 强制全局输出规则（必须严格遵守）

1. 除非明确要求「解释」「讲逻辑」「总结」「展开说明」，否则**只输出可直接使用的代码、终端命令、配置内容**，禁止开场白、客套话、总结句、解释性文字。
2. 输出极致精简，减少 token；能一行写完绝不分行，可省略则省略。
3. 改代码仅给修改后的完整代码块或差异，不另述原因（除非主动要求）。
4. 终端操作仅给可复制运行的命令，不解释作用（除非主动要求）。
5. 代码须遵循本文档「数据模型」字段约定。

# 【全局强制优化】高优先级

## 1. 极致省 Token 与效率

- 与上文「强制全局输出」并列遵守；核心结论 + 可运行代码/命令，删冗余。
- **禁止自动执行耗时命令**（环境体检向）：严禁自动运行 `brew`、`flutter doctor`、**未在对话中约定**的 `pod install` / 全量 `gradle` 等**仅为无目标任务摸底**的操作；环境问题可给结论与**手动**可复制命令。若用户**显式要求**构建/装依赖/修 CI，或完成当前任务**无可替代地**需要其中一步，则允许执行，且必须遵守下文 **Agent Harness**。
- **禁止冗余日志分析**：严禁自动**深度**读取、归纳终端全文；仅当用户明确要求「分析日志」时截取关键片段处理。**例外**：执行 **Agent Harness** 时，允许**只读**终端输出文件**尾部若干行**作进度摘要（不算深度日志分析）。

## 2. 提速与安全

- 默认在沙箱（Auto-Run in Sandbox）执行命令；非必要不申请扩展权限。
- **命令必要化**：仅执行用户指令中的核心命令，不扩展、不全量诊断、不串联无关命令。
- **上下文精准加载**：仅加载本次任务必需信息，不主动加载/解析无关文件。

## 3. Agent Harness（长耗时与可见性）

- **适用命令**（预计 **>60s**）：`flutter build`（含 `ipa` / `apk` / `appbundle`）、`pod install`、`gradle`、长时间 `git clone` 等；以及用户**明确要求**执行的上述命令。
- **禁止长时间零反馈**：若必须执行上述命令，应 **后台运行**（或合理拆分 `block_until_ms` + **轮询**），通过 **Await / 周期性只读** 终端输出文件**末尾**，**每 30～60s**（或日志出现明显阶段变化时）输出**极简一句**状态（如：仍在 `pod install` / 仍在 `xcodebuild` / 已结束）。
- **结束准则**：进程结束后根据**退出码**与**输出末尾关键错误/成功标识**下结论；失败时不得声称成功。
- **verbose**：用户未禁止时，对易卡住步骤可附加 `--verbose`/`-v` 以便定位阶段（仍遵守 Token 与摘要原则，不在正文中粘贴大段日志）。

## 4. 项目上下文（免重复）

- **仓库/产品**：Flutter 服装搭配应用 `wardrobe_app`（AI衣橱助手）。
- **开发机**：MacBook Air M1。后续对话勿重复声明上述基础信息。

# AI衣橱助手

## 项目信息

- **项目名称**：AI衣橱助手
- **技术栈**：Flutter、Riverpod、GoRouter、Isar、Supabase（HTTP 等补充能力可用 Dio，与现有依赖一致即可）

## 架构：Clean Architecture 三层分离

按职责分为 **表示层 / 领域层 / 数据层**，与仓库目录对应关系如下：

| 层级 | 职责 | 代码位置 |
|------|------|----------|
| **表示层** | UI、页面组合、Riverpod 状态 | `lib/presentation/`（`pages`、`widgets`、`providers`） |
| **领域层** | 实体、仓储抽象、用例，不依赖 Flutter 与具体数据源 | `lib/domain/`（`models`、`repositories`、`usecases`） |
| **数据层** | Isar 本地、Supabase/Dio 远程、仓储实现 | `lib/data/`（`local`、`remote`、`repositories`） |

- **core**（`lib/core/`）：主题、路由表、常量、工具等横切能力；不承载具体业务用例实现。
- 表示层通过用例或仓储抽象访问领域；数据层实现领域层定义的仓储接口；**禁止**领域层依赖 `presentation` / `data` 的具体实现。
- 路由统一由 **GoRouter** 配置（建议集中在 `lib/core/router/`）；状态以 **Riverpod** 为主。

## 代码规范

- **注释**：所有注释使用**中文**。
- **文件名**：使用 **snake_case**（如 `clothing_card.dart`）。
- **类名 / 类型名**：使用 **PascalCase**（如 `Clothing`、`UserProfile`）。
- **import 顺序**：Dart SDK → 第三方 package → 相对路径。
- 新增代码放入对应分层目录；复杂逻辑放在 provider / 用例，避免在 Widget 中堆叠数据与业务规则。

## 数据模型（领域字段约定）

以下字段为产品语义约定；实现时类型（如 `DateTime`、`List<String>`、金额用 `int` 分或 `Decimal` 等）在 `domain/models` 中具体定义，并与 Isar / Supabase 映射保持一致。

### Clothing（单品）

`id`，`name`，`category`，`colors`，`brand`，`size`，`imageUrl`，`croppedImageUrl`，`tags`，`season`，`occasion`，`style`，`purchaseDate`，`purchasePrice`，`status`，`usageCount`，`lastWornDate`，`notes`

### Outfit（搭配）

`id`，`name`，`clothingIds`，`scene`，`occasion`，`season`，`imageUrl`，`wornDates`，`plannedDates`，`notes`，`isShared`

### UserProfile（用户档案）

`styles`，`bodyType`，`height`，`weight`，`favoriteColors`，`favoriteOccasions`
