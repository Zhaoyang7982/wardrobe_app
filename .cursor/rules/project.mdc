---
description: 衣橱 Flutter 项目架构与协作约定
globs: lib/**/*.dart
---

# 项目约定

## 分层（Clean Architecture）

- **core**：与应用业务无关的基础设施——常量、主题、`go_router` 路由表、通用工具；不依赖 `data` / `domain` / `presentation` 中的业务实现。
- **domain**：纯 Dart——实体/值对象（`models`）、仓储抽象接口（`repositories`）、用例（`usecases`）；禁止依赖 Flutter、`data` 具体实现。
- **data**：`local`（Isar 等）、`remote`（Dio、Supabase）、`repositories`（实现 domain 仓储接口）；可依赖 `domain`，不直接依赖 UI。
- **presentation**：`pages`、`widgets`、`providers`（Riverpod）；通过用例或仓储抽象访问领域，避免在 Widget 中写复杂数据逻辑。

## 状态与路由

- 全局与跨页状态优先 **Riverpod**（`@riverpod` / `riverpod_annotation` 时配合代码生成工具使用）。
- 导航统一经 **go_router** 配置（建议放在 `lib/core/router/`），避免在业务层散落 `Navigator.push` 字符串路由。

## 数据与后端

- 本地持久化：**Isar**；远程与认证：**Supabase**；HTTP：**Dio**。
- 图表：**fl_chart**；图片：选图 **image_picker**，网络图 **cached_network_image**，编辑 **flutter_image_editor**（按需在具体功能模块封装）。

## 代码风格

- 新增文件放入对应分层目录；保持 import 顺序为 Dart SDK → package → 相对路径。
- UI 与数据解耦：页面组合 widgets，复杂逻辑下沉到 provider / usecase。
