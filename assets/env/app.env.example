# 复制本文件为同目录下的 app.env 并填写真实值；app.env 已被 .gitignore，勿提交。
# GitHub Actions 构建 Web 时通过 Secrets + --dart-define 注入 Supabase，可不依赖本文件里的这两项。

# Supabase（本地 / 非 CI 注入时使用）
SUPABASE_URL=
SUPABASE_ANON_KEY=

# 可选：抠图等
REMOVE_BG_API_KEY=

# 可选：AI 搭配
AI_API_KEY=
AI_API_BASE_URL=
AI_MODEL=gpt-4o-mini
